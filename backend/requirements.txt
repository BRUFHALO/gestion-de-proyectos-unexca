# Paquetes esenciales para la base de datos
motor>=3.3.0
pydantic>=2.5.0
pydantic[email]>=2.5.0
python-dotenv>=1.0.0
python-multipart>=0.0.6

# Framework web (opcional por ahora)
fastapi>=0.109.0
uvicorn>=0.27.0

# Utilidades HTTP
httpx>=0.26.0

# Procesamiento de documentos DOCX y PDF
python-docx>=1.1.0          # Para leer y escribir archivos DOCX
mammoth>=1.6.0              # Para convertir DOCX a HTML limpio
Pillow>=10.0.0              # Para procesamiento de imágenes
reportlab>=4.0.0            # Para generar PDFs con anotaciones
PyMuPDF>=1.23.0             # Para procesamiento avanzado de PDFs y conversión
lxml>=4.9.0                 # Para procesamiento XML/HTML

# Storage en la nube
cloudinary>=1.41.0

# Los siguientes paquetes se instalarán después cuando sean necesarios:
# python-jose[cryptography]  # Para autenticación JWT
# passlib[bcrypt]            # Para hasheo de contraseñas
# celery                     # Para tareas asíncronas
# redis                      # Para cache
